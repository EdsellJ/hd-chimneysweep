import NavLinks from "./nav-links";
const Navbar = () => {
    return (
        <nav className="flex justify-end items-center h-16">
            <NavLinks />
        </nav>
    );
};

export default Navbar;
